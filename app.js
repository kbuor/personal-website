// Khởi tạo AOS (Animate On Scroll)
AOS.init({
  duration: 1000, // Thời gian hiệu ứng
  easing: 'ease-in-out', // Easing function cho chuyển động mượt mà
  once: false // Đảm bảo hiệu ứng xuất hiện mỗi khi phần tử cuộn vào màn hình
});
